---
interface Props {
  class?: string
  hover?: boolean
}

const { class: className, hover = true } = Astro.props
---

<div class:list={[
  'card',
  hover && 'transform hover:-translate-y-1',
  className
]}>
  <slot />
</div>
